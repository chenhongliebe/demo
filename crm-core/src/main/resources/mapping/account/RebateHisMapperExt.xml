<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.frico.crmcore.mapper.RebateHisMapperExt">
  <!-- 扩展自动生成或自定义的SQl语句写在此文件中 -->
  <select id="findList" parameterType="com.frico.crmcore.model.RebateHis" resultMap="BaseResultMap">
    SELECT
    <include refid="Base_Column_List"/>
    FROM frico_crm_rabate_his
    WHERE 1=1
    ORDER BY to_time DESC
  </select>
</mapper>